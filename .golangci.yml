version: "2"
run:
  timeout: 5m
  modules-download-mode: readonly

linters:
  enable:
    - errcheck
    - goconst
    - govet
    - staticcheck
    - unused

issues:
  max-issues-per-linter: 0
  max-same-issues: 0